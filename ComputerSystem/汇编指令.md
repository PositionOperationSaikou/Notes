
63            31            15              7           0
%rax          %eax          %ax             %al                     返回值          
%rbx          %ebx          %bx             %bl                     被调用者保存
%rcx          %ecx          %cx             %cl                     第4个参数
%rdx          %edx          %dx             %dl                     第3个参数
%rsi          %esi          %si             %sil                    第2个参数
%rdi          %edi          %di             %dil                    第1个参数
%rbp          %ebp          %bp             %bpl                    被调用者保存
%rsp          %esp          %sp             %spl                    栈指针
%r8           %r8d          %r8w            %r8b                    第5个参数
%r9           %r9d          %r9w            %r9b                    第6个参数
%r10          %r10d         %r10w           %r10b                   调用者保存
%r11          %r11d         %r11w           %r11b                   调用者保存
%r12          %r12d         %r12w           %r12b                   被调用者保存
%r13          %r13d         %r13w           %r13b                   被调用者保存                
%r14          %r14d         %r14w           %r14b                   被调用者保存 
%r15          %r15d         %r15w           %r15b                   被调用者保存

操作数指示符
1、立即数 $后跟一个c表示法的整数比如$-577或$0x1f.
2、寄存器 %寄存器名称, 将寄存器集合看作数组R, 寄存器标识当作索引.
3、内存引用Imm(rb, ri, s) Imm是立即数偏移, rb是基址寄存器, 
ri是变址寄存器. s是比例因子且其值必须是1、2、4或8, 基址和变址
寄存器必须是64位寄存器, 有效地址被计算为Imm+R[rb] + R[ri] * s.
引用数组元素时, 会用到这种通用形式. 其他形式都是这种形式的特殊情况.

   类型         格式A           操作数值                   名称
   立即数       $Imm            Imm                       立即数寻址
   寄存器       ra              R[ra]                     寄存器寻址
   存储器       Imm             M[Imm]                    绝对寻址
   存储器       (ra)            M[R[ra]]                  间接寻址
   存储器       Imm(rb)         M[Imm+R[rb]]              (基址+偏移量寻址)
   存储器       (rb,ri)         M[R[rb]+R[ri]]            变址寻址
   存储器       Imm(rb,ri)      M[Imm+R[rb]+R[ri]]        变址寻址
   存储器       (,ri,s)         M[R[ri]*s]                比例变址寻址
   存储器       Imm(,ri,s)      M[Imm+R[ri]*s]            比例变址寻址
   存储器       (rb,ri,s)       M[R[rb]+R[ri]*s]          比例变址bi寻址
   存储器       Imm(rb,ri,s)    M[Imm+R[rb]+R[ri]*s]      比例变址寻址


练习题3.1
操作数                  值
%rax                    0x100
0x104                   0xAB
$0x108                  0x108
(%rax)                  0xFF
4(%rax)                 0xAB
9(%rax,%rdx)            0x11
260(%rcx,%rdx)          0x13
0xFC(,%rcx,4)           0xFF
(%rax,%rdx,4)           0x11


数据传送指令
MOV 把数据从源位置传送到目标位置, 不做任何更改.

x86-64增加了限制mov指令不能两个操作数都指向内存位置.
即将一个数从内存位置拷贝到另外一个内存位置, 不能一条指令完成,
必须先拷贝到寄存器, 然后再从寄存器拷贝到内存.

movl会将高位四个字节设置为0, 即任何为寄存器生成32位
值的指令都会把该寄存器的高位部分设置成0.

movq源位置是立即数只能是4字节, 剩下的部分将这个4字节立即数符号扩展得到64位的值,
(即如果是补码实现, 正数前面补充0, 负数需要将这个值转化位64位的相同值(按位取反+1)).
源位置是寄存器或则内存则会完整拷贝64位立即数.

movabsq指令能够以任意64位立即数作位为源操作数, 但只能以寄存器为目的.
这意味着如果需要将64位立即数拷贝到内存, 则需要使用两条指令先movabsq拷贝到寄存器, 再movq拷贝到内存.
这样做的目的是避免过长的指令出现,比如将一个64位立即数拷贝到内存, 指令需要包含这个64位立即数以及计算内存偏移量、比例变地址
等二进制码, 则指令的长度可能是10多个字节.过长的指令会降低cpu的指令缓存命中率, 并且如果都使用movq拷贝64位立即数,
则意味原本使用32位立即数的场景都会扩展64位立即数. 权衡后再部分少见的情况下使用将指令扩展到两条是更好的做法.

movabsq

指令                效果            描述
mov s,d            d<-s            传送
movb                               传送字节
movw                               传送字
movl                               传送双字
movq                               传送四字
movabsq I,R         R<-i           传送绝对的四字


指令                效果                        描述
MOVZ    S, R        R<-零扩展(S)                以零扩展进行传送
movzbw                                          将做了零扩展的单字节传送到单字
movzbl                                          将做了零扩展的单字节传送到双字
movzwl                                          将做了零扩展的单字传送到双字
movzbq                                          将做了零扩展的单字节传送到四字
movzwq                                          将做了零扩展的单字节传送到四字

这些指令只能以寄存器或内存作为源, 以寄存器作位目的

指令                效果                        描述
MOVS S,r            R<-零扩展(S)                以零扩展进行传送
movsbw                                          将做了符号扩展的字节传送到字
movsbl                                          将做了符号扩展的字节传送到双字
movswl                                          将做了符号扩展的字传送到私双字
movsbq                                          将做了符号扩展的字节传送到四字
movswq                                          将做了符号扩展的字传送到四字
movslq                                          将做了符号扩展的双字传送到四字
cltq                %rax<-符号扩展(%eax)         把%eax符号扩展到%rax

同上movs指令只能以寄存器或内存地址作为源, 以寄存器作位目的. cltq指令之作用于寄存器%eax和%rax

练习题3.2
movl    %eax, (%rsp)
movw    (%rax), %dx
movb    $0xFF,  %bl
movb    (%rsp, %rdx, 4), %dl
movq    (%rdx), %rax
movw     %dx, (%rax)

练习题3.3
movb $0xf, (%ebx)       ebx只有双字, 但表示一个指针需要4字
movl %rax, (%rsp)       rax是64位, 但movl只能传送双字
movw (%rax)  4(%rsp)    传送指令不能源和目的位置都是内存地址.
movb %al, %sl           没有sl这个寄存器
movq %rax, $0x123       不能将立即数作位目的地址
movl %eax, %rdx         movl传送双字, 但rdx是四字大小
movb %si, 8(%rbp)       movb传送字节, 但%si是一个字大小

练习题3.4

src_t          dest_t                  指令
long           long                   movq (%rdi) %rax movq %rax (%rsi)
char           int                    movsbl (%rdi) %eax  movl %eax (%rsi)
char           unsigned               movzbl (%rdi) %eax  movl %eax (%rsi)
unsigned char  long                   movzbq (%rdi) %rax  movq %rax (%rsi)
int            char                   movb (%rdi) %al  movb %al (%rsi) 
unsigned       unsigned char          movb (%rdi) %al  movb %al (%rsi)
char           short                  movsbw (%rdi) %ax movw %ax (%rsi)

练习题3.5

void decode1(long *xp, long *yp, long *zp){
    long tx = *xp;
    long ty = *yp;
    long tz = *zp;
    *yp = tx;
    *zp = ty;
    *xp = tz;
}

压入和弹出栈数据
pushq S 将rsp寄存器值减8, 然后传送值S到rsp所指向的栈顶.
popq D 从rsp寄存器值所指向栈顶位置拷贝8字节数据到寄存器D, 然后rsp寄存器值-8. 

这些指令有各种带不同大小操作数的变种(只有leaq没有其他大小变种)。
例如add由四条加法指令组成: addb、 addw、 addl、 addq.
分别是负责字节、字、双字、四字加法.

这些指令被分为四组: 加载有效地址、一元操作、二元操作和位移. 一元操作有一个操作数, 
二元操作有两个操作数. 

图3.10
指令          效果            描述
leaq S,D      D<-&S          加载有效地址
INC D         D<-D+1         加1
DEC D         D<-D-1         减1
NEG D         D<--D          取负
NOT D         D<-~D          取补

ADD S,D       D<-D+S         加
SUB S,D       D<-D-S         减
IMUL S,D      D<-D*S         乘
XOR S,D       D<-D^r         异或
OR  S,D       D<-D|R         或
AND S,D       D<-D&S         与

SAL k,D       D<-D<<k        左移
SHL k,D       D<-D<<k        左移(等同SAL)
SAR k,D       D<-D>>Ak       算数右移
SHR k,D       D<-D>>Ak       逻辑右移

leaq 用法类似movq, 源位置看上去是一个内存地址, 不同的是它根本没有引用内存, 
而是将有效地址写入到目的寄存器.
举个例子 lead(%rdi, %rsi, 4) %rax, 这条指令将%di + rsi + 4的值写入rax

练习题 3.6
表达式                               结果
leaq 6(%ax), %rdx                   (x&0xffff)+6
leaq (%rax, %rcx), %rdx             x+y
leaq (%rax, %rcx, 4), %rdx          x+4*y
leaq 7(%rax, %rax, 8), %rdx         7+9x
leaq 0xA(,%rcx, 4), %rdx            0xA+y*4
leaq 9(%rax, %rcx, 2), %rdx         9+x+2*y

练习题3.7
5x+2y+8z

一元和二元操作
一元操作只有一个操作数, 即是源又是目的. 这个操作数既可以是一个寄存器也可以是一个内存地址.
二元操作第二个操作数即是源又是目的, 但不能两个操作数都是内存地址引用.

指令                                目的        值
addq %rcx,(%rax)                    0x100      0x100
subq %rdx, 8(%rax)                  0x108      0xA8
imulq $16, (%rax, %rdx, 8)          0x118      0x110
incq 16(%rax)                       0x110      0x14
decq %rcx                           %rcx       0x0
subq %rdx, %rax                     %rax       0xFD

位移操作
位移操作对w位字长的数据进行操作, 位移量是由%cl寄存器的低m位决定的, 这里2^m=w.
即%cl中取出的要位移的位数最大值位w-1.

练习题3.9
salq $4 %rax
sarq %cl %rax

练习题3.10

long t1 = y|x;
long t2 = t1 >> 3;
long t3 = ~t2;
long t4 = z - r3;

练习题3.11

A、将%rdx中值设置位0;
B、movq $0, %rdx;
C、movq $0, %rdx指令是7字节, 而xorq %rdx, %rdx也是3字节,
减少了3字节.

一元的乘法运算, 用来计算两个64位值的全128位乘积, 
第一个参数保存在寄存器rax中, 另外一个做为操作数给出.
然后计算结果存在寄存器rdx(高64位)和rax(低64位)中.

一元的除法运算, 用来计算商和余数, 
第一个参数是8字保存在寄存器rdx(高64位)和寄存器rax(低64位)中, 
所以计算前需要将要将要计算的被除数拷贝到rax, 然后使用clto指令
进行符号扩展(填充rdx为符号位).
另外一个作为操作数给出, 
计算结果保存在寄存器rax(商), rdx(余数)中.

指令       效果                                     描述
imulq S    R[%rdx]:R[%rax] <- S*R[%rax]            有符号全乘法
mulq S     R[%rdx]:R[%rax] <- S*R[%rax]            无符号全乘法
clto S     R[%rdx]:R[%rax] <- 符号扩展(R[%rax])     转换8字
idivq S    R[%rax]<-R[%rdx]:R[%rax] / S            有符号除法
           R[%rdx]<-R[%rdx]:R[%rax] mod S
divq S     R[%rax]<-R[%rdx]:R[%rax] / S            无符号除法
           R[%rdx]<-R[%rdx]:R[%rax] mod S

练习题3.12
x in %rdi, y in %rsi, qp in %rdx, rp in %rcx
movq %rdx %r8
movq %rdi %rax
xorq %rdx %rdx
divq %rsi
movq %rax (%r8)
movq %rdx (%rcx)

条件码
描述最近的算数或逻辑操作的属性.

CF: 进位/借位标志, 最近运算溢出宽度（即需要当前最高位高一位发生进位或则借位）才会值为1
ZF: 零标志, 最近的操作得出的结果为0
SF: 符号标志. 最近的操作得到的结果为负数.
OF: 溢出标志. 最近的操作导致一个补码溢出.

leaq指令不改便任何条件码,  因为它是用来计算地址的.
除此之外图3.10所有的操作都会设置条件码. 
逻辑操作设置进位标志和溢出标志为0. 
位移操作进位标志设置为最后一个被移出的位, 溢出标志设置为0. 
INC和DEC会设置溢出标志和零标志, 但不改变进位标志.


cmp S, D类指令有cmpb、cmpw、 cmpl、cmpq. 行为类似SUB S, D, 
唯一的不同在于它不保存结果, 只通过计算后条件码状态完成比较, 分析如下 

相等b=a, b-a=0, zf=1

无符号
只需要判断cf, b < a则 b-a发生借位cf=1否则为0; 

有符号
cmpq a,b  如果b>a,  则b-a>0， 
情况1 a>0, b >0 则sf=0, of=0;  
情况2 b<0, a <0(符号位抵消, b的后面部分大于a结果是正数不溢出)， sf = 0, of = 0. 
情况3 b>0, a<0（a符号位1，b为0 
此时结果不溢出b(0001 - 1100)= b(0001 + 0100) = b(0101)此时of=0, sf=0
溢出 b(0111 - 1000) = b(0111 + 1000) = b(1111) 此时of=1 sf=1, 即溢出后计算结果符号发生反转.


test S, D类指令有testq、 testw、 testl、 testq. 行为类似and s, d.
唯一的不同在于它不保存结果, 只通过计算后条件码状态完成判断.
cf和of置0, 当计算结果为0是zf=1, 计算结果符号位为1sf=1.
用法是S需要检测的值, D掩码用来标记需要检测的位.
完成它只需要一个cpu时钟周期(更快更无负担),
所以经常用于替换cmp和and检测一个值是否为0 test b(0000), 
b(0000) 或则是否为负 test b(1000), b(1000).

访问条件码
set指令用于根据条件码状态设置一个字节的寄存器或则内存.

指令            同义名              效果                设置条件
sete D          setz               D<-zf                相等/零
setne D         setnz              D<-~zf               不等/非零
sets D                             D<-sf                负数
setns D                            D<-~sf               非负数
setg D          setnle             D<-~(SF^OF)&~ZF      有符号>
setge D         setnl              D<-~(SF^OF)          有符号>=
setl D          setnge             D<-SF^OF             有符号<
setle D         setng              D<-(SF^OF)|ZF        有符号<=
seta D          setnbe             D<-~CF&~ZF           无符号>
setae D         setnb              D<-~CF               无符号>=
setb D          setnae             D<-CF                无符号<
setbe D         setna              D<-CF|ZF             无符号<=

练习题 3.13
A、typedef int data_t; #define COMP <
B、typedef short data_t; #define COMP  >
C、typedef unsigned char data_t; #define COMP <=
D、
情况1: typedef unsigned long data_t; #define COMP !=
情况2: typedef long data_t; #define COMP !=

练习题 3.13
A、
typedef long date_t; #define TEST >=

B、
情况1 typedef short date_t; #define TEST !=
情况2 typedef unsigned short date_t; #define TEST !=
C、 


